@* Shared CSS (head) *@
<link rel="icon" type="image/x-icon" href="~/assets/logo/WiHR/Logo.png">
<link rel="stylesheet" href="~/assets/lib/iconsFont/css/all.min.css">
<link rel="stylesheet" href="~/assets/lib/bootstrap/css/bootstrap.min.css">
<link rel="stylesheet" href="~/assets/lib/swiper/css/swiper-bundle.min.css">
<link rel="stylesheet" href="~/assets/lib/select2/select2.min.css">
<link rel="stylesheet" href="~/assets/lib/video-js/css/video-js.css">
<link rel="stylesheet" href="~/assets/css/main.css" asp-append-version="true">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet" />
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" />
@* 
<style>
    :root {
        --primary: #6a5cff;
        --secondary: #6bd4ff;
        --peach: #ff7a59;
        --text: #1f1f1f;
        --muted: #6c757d;
        --card-radius: 1.25rem;
    }

    body {
        font-family: "Tajawal", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji","Segoe UI Symbol";
        color: var(--text);
        background: #fafbfc;
    }
    /* Top bar */
    .topbar {
        font-size: .875rem;
        background: #fff;
        border-bottom: 1px solid rgba(0,0,0,.05);
    }

        .topbar a {
            text-decoration: none;
            color: #495057
        }

    .badge-soft {
        background: rgba(106,92,255,.08);
        color: var(--primary);
        border: 1px solid rgba(106,92,255,.15)
    }
    /* Hero */
    .hero {
        position: relative;
        color: #fff;
        overflow: hidden;
        background: radial-gradient(1200px 600px at 80% -20%, rgba(107,212,255,.35), transparent 60%), radial-gradient(1000px 800px at -10% -10%, rgba(106,92,255,.45), transparent 60%), #101928;
    }

        .hero:before {
            content: "";
            position: absolute;
            inset: 0;
            background: url('https://images.unsplash.com/photo-1544989164-31dc3c645987?q=80&w=1920&auto=format&fit=crop') center/cover no-repeat;
            opacity: .25;
            filter: saturate(1.2) contrast(1.05);
        }

    .hero-curve {
        position: absolute;
        inset: auto 0 -1px 0;
        height: 140px;
        background: radial-gradient(100% 140px at 50% 0, #fff 0, #fff 60%, transparent 61% );
        mask: radial-gradient(100% 140px at 50% 0, #000 60%, transparent 61%);
    }

    .hero .lead {
        color: #e7ecf3
    }

    .btn-light-gradient {
        background: linear-gradient(135deg, #fff, #eef3ff);
        border: 1px solid rgba(255,255,255,.6);
    }

    /* Cards */
    .card-soft {
        border: 1px solid rgba(0,0,0,.06);
        border-radius: var(--card-radius);
        box-shadow: 0 .25rem 1.25rem rgba(16,25,40,.06)
    }

        .card-soft .card-img-top {
            height: 210px;
            object-fit: cover;
            border-top-left-radius: var(--card-radius);
            border-top-right-radius: var(--card-radius)
        }

    .section-title {
        font-weight: 800
    }

    .section-pad {
        padding: 4rem 0
    }
    /* Swiper */
    .swiper {
        padding: .25rem 0 2.5rem
    }

    .swiper-button-next, .swiper-button-prev {
        width: 44px;
        height: 44px;
        border-radius: 50%;
        background: #fff;
        border: 1px solid rgba(0,0,0,.08);
        box-shadow: 0 .25rem .75rem rgba(0,0,0,.08);
        color: #333;
    }

        .swiper-button-next:after, .swiper-button-prev:after {
            font-size: 18px;
            font-weight: 800
        }

    .swiper-pagination-bullet {
        background: #c7c9d3;
        opacity: 1
    }

    .swiper-pagination-bullet-active {
        background: var(--primary)
    }
    /* Features band */
    .feature-dot {
        width: 10px;
        height: 10px;
        background: var(--primary);
        border-radius: 50%
    }

    .feature-band {
        background: #fff;
        border-top: 1px solid rgba(0,0,0,.05);
        border-bottom: 1px solid rgba(0,0,0,.05)
    }

    .icon-pill {
        width: 38px;
        height: 38px;
        border-radius: 999px;
        background: rgba(106,92,255,.12);
        display: inline-flex;
        align-items: center;
        justify-content: center;
        color: var(--primary)
    }
    /* Footer */
    .footer {
        color: #7b8a9b;
        font-size: .95rem
    }

        .footer a {
            color: inherit;
            text-decoration: none
        }

    .social a {
        width: 36px;
        height: 36px;
        border-radius: 999px;
        border: 1px solid rgba(0,0,0,.08);
        display: flex;
        align-items: center;
        justify-content: center
    }
    /* Small helpers */
    .w-48 {
        width: 48px
    }

    .h-48 {
        height: 48px
    }
</style>
 *@

<!-- custom CSS -->
<link rel="stylesheet" href="~/assets/css/main.min.css" asp-append-version="true">